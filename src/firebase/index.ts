'use client';

import { firebaseConfig } from '@/firebase/config';
import { initializeApp, getApps, getApp, FirebaseApp } from 'firebase/app';
import { getAuth } from 'firebase/auth';
import { getFirestore } from 'firebase/firestore'

// IMPORTANT: DO NOT MODIFY THIS FUNCTION
export function initializeFirebase() {
  if (getApps().length) {
    // If already initialized, return the SDKs with the already initialized App
    return getSdks(getApp());
  }

  // When deployed to Firebase App Hosting, these environment variables are automatically provided.
  // In a local development environment, they are not present, and we fall back to the config object.
  const isFirebaseHosting = process.env.NEXT_PUBLIC_FIREBASE_APP_ID;
  let firebaseApp;

  if (isFirebaseHosting) {
    // In a deployed environment, initialize without a config object.
    firebaseApp = initializeApp();
  } else {
    // In a local environment, initialize with the config object.
    firebaseApp = initializeApp(firebaseConfig);
  }

  return getSdks(firebaseApp);
}

export function getSdks(firebaseApp: FirebaseApp) {
  return {
    firebaseApp,
    auth: getAuth(firebaseApp),
    firestore: getFirestore(firebaseApp)
  };
}

export * from './provider';
export * from './client-provider';
export * from './firestore/use-collection';
export * from './firestore/use-doc';
export * from './non-blocking-updates';
export * from './non-blocking-login';
export * from './errors';
export * from './error-emitter';